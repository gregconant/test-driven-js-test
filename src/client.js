/*global define, describe, it, expect, dump, window, Raphael, $, alert, wikiPaint:true */

define([], function() {
  "use strict";

  var paper,
      self = {},
      relativeOffset,
      drawLine;

  self.message = "some thing";
  return self;
});